{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna run --parallel genPackagelock"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.3.0"
  }
}
